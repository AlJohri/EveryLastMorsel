<!-- <p class="buttons-set">
    <a href="http://alpha.everylastmorsel.com/profile/about-edit/" class="btn wider btn-orange">
        <span>Edit About</span>
    </a>
</p> -->

<article>

    <ul>
        <% @posts.each do |post| %>
            <li >
            	<h2> <%= post.title %> </h2>
                <span style="line-height: 1.35px;"><%= truncate(post.text, :length => 255)%> </span> </br>
                <%= link_to "Read More...", post_url(post.id) %>
            </li>
        <% end %>
    </ul>

</article>