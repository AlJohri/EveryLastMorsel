= simple_form_for @crop, :validate => true, :url => polymorphic_url([parent, :crops])  do |f|
  = f.input :plot_id, collection: current_user.plots, prompt: "Choose your plot"
  = f.input :crop_name, input_html: { data: { behaviour: "autocomplete", autocomplete_source: "/crops.json" } }
  = f.input :variety_name, input_html: { data: { behaviour: "autocomplete", autocomplete_source: "/varieties.json" } }
  = f.input :plant_date, :as => :string, :input_html => { data: { behaviour: "datepicker" } }
  = f.button :submit